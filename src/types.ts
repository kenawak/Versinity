export interface Verse {
    id: string
    text: string
    reference: string
    imageUrl: string
    commentary?: string
  }
  
  export interface FontSettings {
    fontFamily: string
    fontSize: string
  }
  
  